# =============================================================================
# Domain Intel Scanner - Environment Configuration
# =============================================================================

# Application Settings
DNS_TIMEOUT=5.0
HTTP_TIMEOUT=10.0
LOG_LEVEL=INFO
FLASK_ENV=production
FLASK_DEBUG=False
SECRET_KEY=your-secret-key-here-change-this

# Security Configuration
CORS_ORIGINS=https://api.domainintelscanner.com
API_SIGNING_REQUIRED=False
RATE_LIMIT_DEFAULT=200 per day, 50 per hour

# Server Settings
#
PORT=5000

# =============================================================================
# API Keys Configuration
# =============================================================================
# API keys are stored in a separate file for security
# The file path below points to the keys file (excluded from git)
API_KEYS_FILE=/srv/git/dis/api_keys.conf
